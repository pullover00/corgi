out/
__pycache__/
*.pyc
.DS_Store

# Machine-local symlinks to large checkpoints / vendored sources -- see
# SETUP.md for how to recreate them (scripts/fetch_assets.sh automates most
# of it).
/checkpoints/*.pt
/checkpoints/*.pth
/src/dinov2
/external/

# Dataset and generated output -- large (data/ ~20GB, results/ ~9GB, rc3d/
# ~170MB on the machine this was authored on) and either reproducible from
# scripts or not this repo's to redistribute. See SETUP.md for how to obtain
# data/PASLCD.
/data/
/results/
/rc3d/
