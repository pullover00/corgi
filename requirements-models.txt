# Validated inference dependencies for the pinned MASt3R/DUSt3R source tree.
# Do not include the upstream research requirements files here: DUSt3R pins
# torch 2.0/CUDA 11-era packages that conflict with the measured headline
# runtime (torch 2.5.1/CUDA 12.4). Optional demos/notebooks are deliberately
# excluded from this production inference lock.
scikit-learn==1.9.0
roma==1.5.6
opencv-python==4.10.0.84
einops==0.8.2
trimesh==4.12.2
matplotlib==3.11.1
huggingface-hub==1.24.0
safetensors==0.8.0
hydra-core==1.3.4
omegaconf==2.3.1
iopath==0.1.10
pyglet==1.5.31

# SAM2 is source-pinned; its numerical source/YAML tree is additionally hashed
# into every full-evaluation run manifest.
sam-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4

# DINOv2 (checked out by bootstrap_models.sh into src/dinov2) is imported
# directly from source by src/ocmask/adapters/dinov2.py and has no separate
# packaging step here; install its own requirements file if you hit missing
# imports:
#   python -m pip install -r src/dinov2/requirements.txt

# SAM3 is an external dependency not covered by this file. Its exact source
# tree and consumed checkpoint are hashed by the evaluator; see README.md's
# "External SAM3 dependency" section. The production run_pair path does not
# load SAM3.1.
